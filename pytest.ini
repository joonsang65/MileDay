[pytest]
testpaths = tests
pythonpath = backend/app
addopts =
    -m "not integration"
    --cov=backend/app
    --cov-report=term-missing
    --cov-fail-under=90
markers =
    integration: tests that use the real Supabase project
