# 사용자 피드백 기반 변경 이력

이 문서는 MileDay `v0.1.0`부터 `v0.1.3`까지 사용자 피드백을 바탕으로 수정한 UI/UX 및 동작 개선 사항을 정리한다.

## 기준

- `git tag`로 남아 있는 릴리스 태그는 없다.
- 버전 구분은 `frontend/package.json`의 `version` 변경과 커밋 순서를 기준으로 정리한다.
- 현재 커밋된 HEAD의 `frontend/package.json` 버전은 `0.1.2`이고, 작업트리에는 `0.1.3` 버전 변경만 미커밋 상태로 존재한다.
- 따라서 `v0.1.3`은 아직 기능 변경이 커밋으로 확정된 릴리스가 아니라, 다음 배포 후보 버전으로 본다.

## 요약

| 피드백 | 최초 반영 버전 | 주요 후속 반영 | 대표 커밋 |
|---|---|---|---|
| 헤더/달력 패널을 sticky로 고정 | `v0.1.1` | - | `9977582 fix(ui): 스크롤 시 달력이 상단에 고정되도록 개선` |
| 전체 목표를 볼 수 있는 패널 추가 | `v0.1.1` | `v0.1.2`에서 통합 조회 API 적용 | `5452c95 feat(ui): 전체 목표 관리 모달 및 다중 삭제 기능 구현`, `8fdb6a3 perf(frontend): 전체 목표 모달 통합 조회 적용` |
| 글자 크기를 더 작게 조정 | `v0.1.1` | `v0.1.2`에서 설정/기본값 정리 | `79268eb fix(settings): 기본 글자 크기 및 목표 글자 크기 범위 1~25px로 수정`, `ad9076d refactor(settings): streamline preferences and panel defaults` |
| 노트북 화면 크기별 UI 깨짐 개선 | `v0.1.2` | - | `ad9076d refactor(settings): streamline preferences and panel defaults` |
| 목표 색상 변경 후 마일스톤/달력 색상 미연동 | `v0.1.1` | `v0.1.2`에서 전체 목표 조회 캐싱/조회 고도화 | `81c014c fix(sync): 목표 색상 수정 시 마일스톤 일괄 동기화 로직 추가`, `8fdb6a3 perf(frontend): 전체 목표 모달 통합 조회 적용` |
| 마일스톤 완료 클릭 후 일시적으로 목표 없음처럼 보임 | `v0.1.0` | `v0.1.2`에서 목표 완료 동기화/RPC 보강 | `e8a31ac feat(frontend): 위젯 UI와 다국어 흐름 정리`, `afeb7a8 feat(frontend): 목표 완료 체크 UI 추가`, `78a26db feat(goals): 목표 통합 조회와 완료 RPC 추가` |
| 하루 보기 패널이 아래에 있어 스크롤이 필요함 | `v0.1.2` | - | `698c06a feat(day-view): open selected dates in floating panel` |

## v0.1.0

### 문제 사항 (피드백)

초기 위젯 UI에서는 마일스톤 완료를 누른 뒤 서버 응답과 재조회가 끝나기 전까지 화면 상태가 흔들릴 수 있었다. 사용자가 보는 관점에서는 방금 완료 처리한 목표나 마일스톤이 잠시 사라지거나, 해당 날짜가 목표 없음 상태처럼 보이는 문제가 있었다.

### 변경 과정

마일스톤 완료 토글 시 서버 응답을 기다린 뒤 전체 데이터를 다시 반영하는 방식에서, 먼저 렌더러 캐시를 갱신하고 실패 시 롤백하는 방식으로 바꿨다. `App.tsx`의 `handleToggleMilestone` 흐름에서 월간/주간 캘린더 캐시와 날짜 상세 캐시를 즉시 갱신하고, API 호출이 실패하면 이전 완료 상태로 되돌리는 구조를 적용했다.

### 변경 결과

마일스톤 완료 클릭 직후 UI가 먼저 반응하게 되었고, 네트워크 지연 중에도 목표/마일스톤 목록이 비어 보이는 현상이 줄었다. 이후 `v0.1.2`에서 목표 완료 상태와 하위 마일스톤 완료 상태 동기화가 추가되면서 완료 처리 정합성도 보강됐다.

### 관련 문서

- [성능 개선 리포트 v1](performance_report._v1.md)
- [성능 개선 리포트 v2](performance_report._v2.md)
- [전체 목표 조회 및 완료 처리 최적화](decisions/0021-전체_목표_조회_및_완료_처리_최적화.md)

## v0.1.1

### 문제 사항 (피드백)

스크롤을 내릴 때 헤더와 달력의 기준 영역이 함께 밀려나 현재 날짜 맥락을 놓치기 쉬웠다. 또한 목표를 한눈에 볼 수 있는 전역 패널이 없어 날짜별 상세 화면을 오가야 했고, 설정에서 글자 크기를 더 작게 조정하고 싶다는 요구도 있었다.

전체 목표 모달에서 목표 색상을 변경해도 이미 생성된 마일스톤과 달력 일부 뷰에는 예전 색상이 남아 있었다. 목표 색상이 사용자 기준으로는 하나의 속성인데, 화면과 DB에서는 부모 목표 색상과 마일스톤 색상이 분리되어 보이는 문제가 있었다.

### 변경 과정

헤더/달력 영역은 스크롤 상황에서도 상단에 고정되도록 CSS를 조정했다. 전체 목표 관리를 위해 `GoalListModal`을 추가하고, 목표 목록, 하위 마일스톤, 다중 삭제 흐름을 한 패널에서 다룰 수 있게 했다.

글자 크기는 설정 패널의 슬라이더와 Electron main process의 정규화 범위를 맞췄다. 기존에는 main process의 `normalizeFontSize()`가 최소값을 10으로 고정하고 있어 UI에서 더 작은 값을 선택해도 저장/재시작 후 10으로 되돌아갔다. 이후 범위를 `1~25px`로 통일했다. 사용자가 말한 "더 작게, 최소 10 수준" 요구는 이 변경에서 더 넓은 범위까지 확장되어 반영됐다.

목표 색상 동기화는 백엔드와 프론트 양쪽에서 보강했다. 백엔드에서는 목표 색상 수정 시 하위 마일스톤 색상도 일괄 갱신하도록 하고, 프론트에서는 달력 렌더링 시 마일스톤 자체 색상보다 부모 목표의 최신 색상을 우선 참조하도록 했다.

### 변경 결과

스크롤 중에도 주요 캘린더 맥락이 유지되고, 전체 목표 모달을 통해 목표와 마일스톤을 한 번에 확인할 수 있게 됐다. 글자 크기 설정은 UI, IPC, 로컬 저장값의 범위가 맞춰져 작은 위젯 화면에서도 더 촘촘한 표시가 가능해졌다.

목표 색상 변경은 달력과 하루 보기에서 즉시 일관되게 보이도록 개선됐다. DB에는 하위 마일스톤 색상도 맞춰 저장되고, 프론트 렌더링은 부모 목표 색상을 단일 진실 원천으로 사용한다.

### 관련 문서

- [트러블 슈팅 기록 - 설정 글자 크기 범위 적용 불가 문제](troubleshooting.md#19-설정-글자-크기-범위-적용-불가-문제)
- [트러블 슈팅 기록 - 목표 및 마일스톤 색상 동기화 불일치 문제](troubleshooting.md#21-목표-및-마일스톤-색상-동기화-불일치-문제)
- [UI 설정 동기화 및 폰트 크기 정규화 범위 조정](decisions/0016-UI_글자_크기_설정_동기화.md)
- [메인 레이아웃 전체 스크롤 및 팝업 표시 수정](decisions/0019-메인_레이아웃_전체_스크롤_및_팝업_수정.md)

## v0.1.2

### 문제 사항 (피드백)

사용자 노트북의 물리적 화면 크기, 해상도, Windows 배율, Electron 창 크기 조합에 따라 UI가 압축되거나 잘리는 문제가 있었다. 특히 작은 세로 높이에서는 하루 보기와 설정/생성 패널 접근성이 떨어졌다.

하루 보기 패널이 달력 아래쪽에 위치해 날짜를 누른 뒤 당일 일정을 확인하려면 스크롤해야 하는 흐름도 덜 직관적이었다. 또한 전체 목표 화면은 목표 목록을 가져온 뒤 각 목표별 하위 마일스톤을 순차 조회해 목표 수가 늘어날수록 로딩 비용이 커졌다.

### 변경 과정

반응형 크기 정책을 문서화하고, `px`, `%`, `vh/vw`, `min()`, `clamp()`, `minmax(0, 1fr)`의 역할을 재정리했다. overlay, floating panel, modal처럼 viewport 경계에 맞춰야 하는 요소에는 `vw/vh`와 `clamp()`를 적용하고, 아이콘과 체크박스처럼 고정 크기가 더 안정적인 요소는 `px`를 유지하는 방향으로 정리했다.

날짜 선택 흐름은 선택한 날짜의 하루 보기를 floating panel로 열도록 바꿨다. 사용자는 달력 날짜를 누른 뒤 아래로 스크롤하지 않고 같은 화면 맥락에서 당일 목표와 마일스톤을 확인할 수 있다.

전체 목표 모달은 `GET /goals` 후 목표별 마일스톤 API를 반복 호출하던 구조에서 `GET /goals/with-milestones` 통합 API를 사용하는 구조로 바꿨다. 백엔드는 목표 전체 조회 1회, 마일스톤 전체 조회 1회 후 `goal_id` 기준으로 병합한다. 완료 처리도 RPC로 묶어 목표와 하위 마일스톤의 완료 상태 동기화를 안정화했다.

### 변경 결과

작은 노트북 화면과 고배율 환경에서도 주요 패널이 잘리거나 겹치는 위험이 줄었다. 날짜 클릭 후 당일 일정을 floating panel에서 바로 확인할 수 있어 하루 보기 접근성이 좋아졌다.

전체 목표 조회는 목표별 순차 API 호출을 제거했다. 성능 측정 기준으로 기존 전체 목표 조회 P95 `25,841.67ms`가 통합 API 적용 후 `1,003.54ms`로 줄었다. 목표 완료 처리도 RPC 적용 후 P95 `1,701.87ms`에서 `1,172.69ms`로 개선됐다.

### 관련 문서

- [물리 화면 대응 크기 조정 및 중복 정의 정리](decisions/0020-물리_화면_대응_크기_조정_및_중복_정리.md)
- [전체 목표 조회 및 완료 처리 최적화](decisions/0021-전체_목표_조회_및_완료_처리_최적화.md)
- [트러블 슈팅 기록 - 목표 편집 날짜 제한 문제](troubleshooting.md#18-목표-편집-날짜-제한-문제)
- [트러블 슈팅 기록 - 목표 및 마일스톤 색상 동기화 불일치 문제](troubleshooting.md#21-목표-및-마일스톤-색상-동기화-불일치-문제)

## v0.1.3

### 문제 사항 (피드백)

현재 작업트리에서는 `frontend/package.json`과 `frontend/package-lock.json`의 버전만 `0.1.3`으로 올라가 있다. 아직 `v0.1.3`에 해당하는 기능 변경 커밋이나 릴리스 태그는 확인되지 않는다.

### 변경 과정

`v0.1.3`은 현 시점에서 다음 배포를 위한 버전 번호 준비 상태로 본다. 기능 변경을 이 버전에 귀속하려면 버전 변경과 함께 해당 변경 커밋 또는 릴리스 태그를 남겨야 한다.

### 변경 결과

문서 기준으로는 `v0.1.3`에 확정 귀속된 사용자 피드백 수정 항목은 없다. 현재까지 확인 가능한 사용자 피드백 반영은 `v0.1.0~v0.1.2` 범위에 있다.

### 관련 문서

- [전체 목표 조회 및 완료 처리 최적화](decisions/0021-전체_목표_조회_및_완료_처리_최적화.md)

## 관련 문서 링크 모음

| 문서 | 내용 |
|---|---|
| [트러블 슈팅 기록](troubleshooting.md) | 실제 사용자 피드백, 증상, 원인, 대응 내역 |
| [성능 개선 리포트 v1](performance_report._v1.md) | 초기 성능 문제, 마일스톤 토글 낙관적 업데이트 필요성 |
| [성능 개선 리포트 v2](performance_report._v2.md) | 캐시 갱신, 중복 조회 제거, 낙관적 UI 갱신 결과 |
| [성능 개선 리포트 v3](performance_report._v3.md) | API 5xx 재시도와 성능 검증 |
| [ADR 0016: UI 설정 동기화 및 폰트 크기 정규화 범위 조정](decisions/0016-UI_글자_크기_설정_동기화.md) | 글자 크기 설정 범위와 IPC 저장값 동기화 |
| [ADR 0019: 메인 레이아웃 전체 스크롤 및 팝업 표시 수정](decisions/0019-메인_레이아웃_전체_스크롤_및_팝업_수정.md) | 전체 스크롤, sticky 흐름, 팝업 clipping 개선 |
| [ADR 0020: 물리 화면 대응 크기 조정 및 중복 정의 정리](decisions/0020-물리_화면_대응_크기_조정_및_중복_정리.md) | 노트북 화면 크기별 UI 깨짐 대응과 반응형 크기 정책 |
| [ADR 0021: 전체 목표 조회 및 완료 처리 최적화](decisions/0021-전체_목표_조회_및_완료_처리_최적화.md) | 전체 목표 통합 조회 API, 완료 처리 RPC, 성능 비교 |
